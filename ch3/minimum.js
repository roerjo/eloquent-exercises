//Write a function min that takes two arguments and returns their minimum.

// My solution:

function min(a, b) {
  return (a < b) ? a : b;
}

/*
    Eloquent Solution:

    function min(a, b) {
        if (a < b) return a;
        else return b;
    }
*/
